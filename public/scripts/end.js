page("Home") // Go to home page


login(2) // Try to login with cookie

openPopup("login-popup") // Open popup (twice)
openPopup("login-popup")


$('account-box').style.display = "none" // Hide boxes
$('notify-box').style.display = "none"

window.scrollTo(0, 1);
